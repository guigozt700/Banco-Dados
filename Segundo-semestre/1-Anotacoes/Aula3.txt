O que é uma view?

Uma view (em português: visão) é como uma “janela” para os dados de uma ou mais tabelas.

👉 Em vez de criar uma nova tabela, você cria uma view que mostra os dados de uma forma específica — normalmente usando uma consulta SQL (SELECT).

Ela não guarda dados de verdade (na maioria dos casos).
Ela mostra os dados existentes de um jeito diferente.

💡 Analogia simples

Imagine que o banco de dados é uma planilha gigante com muitas abas (tabelas).
Uma view é como uma aba especial que mostra só as colunas e linhas que você quer ver — sem copiar os dados originais.

📋 Exemplo prático

Suponha que temos uma tabela chamada clientes:

| id | nome  | cidade    | idade | saldo |
| -- | ----- | --------- | ----- | ----- |
| 1  | Ana   | São Paulo | 30    | 500   |
| 2  | Bruno | Rio       | 25    | 1200  |
| 3  | Carla | São Paulo | 40    | 800   |

E queremos ver somente os clientes de São Paulo.

Podemos criar uma view assim:

CREATE VIEW clientes_sp AS
SELECT nome, idade, saldo
FROM clientes
WHERE cidade = 'São Paulo';


Agora, quando você quiser ver só esses clientes, basta usar:

SELECT * FROM clientes_sp;

| nome  | idade | saldo |
| ----- | ----- | ----- |
| Ana   | 30    | 500   |
| Carla | 40    | 800   |


Resumo: o que é uma view?

| Conceito                            | Explicação                                                                                      |
| ----------------------------------- | ----------------------------------------------------------------------------------------------- |
| **É baseada em SELECT**             | A view é construída a partir de uma consulta.                                                   |
| **Não armazena dados (geralmente)** | Ela mostra os dados das tabelas originais.                                                      |
| **Facilita consultas**              | Você pode criar views para consultas complexas e depois usá-las como se fossem tabelas simples. |
| **Aumenta a segurança**             | Pode mostrar só certas colunas ou linhas, escondendo dados sensíveis.                           |
| **Atualizável (às vezes)**          | Algumas views permitem `INSERT`, `UPDATE`, e `DELETE`, mas com restrições.                      |


Exemplo mais útil

Digamos que você tem várias tabelas:

clientes

pedidos

produtos

E quer ver um “resumo de vendas por cliente”.
Em vez de escrever um JOIN complicado toda vez, você pode criar uma view:

CREATE VIEW resumo_vendas AS
SELECT c.nome AS cliente,
       COUNT(p.id) AS total_pedidos,
       SUM(p.valor) AS total_gasto
FROM clientes c
JOIN pedidos p ON c.id = p.id_cliente
GROUP BY c.nome;


Agora, basta:

SELECT * FROM resumo_vendas;

🔐 Benefícios de usar views

✅ Simplificam consultas complexas
✅ Melhoram a segurança (ocultam dados sensíveis)
✅ Facilitam a manutenção do código
✅ Podem servir de “camada lógica” entre a aplicação e o banco

⚠️ Cuidados

⚠️ Views não melhoram desempenho por si só (elas não são índices).
⚠️ Algumas views complexas podem deixar as consultas mais lentas, pois precisam processar o SELECT original toda vez.
⚠️ Views atualizáveis têm restrições — por exemplo, não podem envolver GROUP BY, JOIN, ou funções agregadas.
